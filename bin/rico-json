#!/usr/bin/env ruby
# -*- coding: utf-8 -*-

require 'rico_json'

args = ARGV.dup


if args.length > 0
  file = args[0]
  json = RicoJSON.read_file(file)
else
  RicoJSON.read_string(STDIN.read)
  # Explain how to use it
end
